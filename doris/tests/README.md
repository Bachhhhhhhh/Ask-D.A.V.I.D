# Goal 6 static serving contract

These files are intentionally secret-free and cannot be used as ad-hoc shell
commands. Connected verification later uses the Terraform-owned private ECS
tasks. The only external source is the allowlisted Unity Catalog Business table;
all objects under `ask_david_serving_development` are disposable copies.
