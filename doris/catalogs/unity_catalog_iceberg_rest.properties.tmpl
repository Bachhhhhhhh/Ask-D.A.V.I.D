# Rendered only by the one-off admin refresh task. Do not commit token values.
# The temporary external catalog is removed after the controlled refresh.
type=iceberg
iceberg.catalog.type=rest
iceberg.rest.uri=https://${DATABRICKS_WORKSPACE_HOST}/api/2.1/unity-catalog/iceberg-rest/
warehouse=ask_david_development
iceberg.rest.security.type=oauth2
iceberg.rest.oauth2.token=${DATABRICKS_OAUTH_ACCESS_TOKEN}
iceberg.rest.vended-credentials-enabled=true
s3.endpoint=https://s3.ap-southeast-1.amazonaws.com
s3.region=ap-southeast-1
